#include "main.h"

/**
 * _islower - program that checks for loercase
 * @c: the character to check
 * Return: 1 if c is lowercase, else 0
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
